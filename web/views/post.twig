{% extends "template/base.twig" %}

{% block title %}Home{% endblock %}
{% block content %}
	<div class="page-header">
		<h1>{{ post.title|e }}</h1>
	</div>
	
	<div class="post-date small">{{ post.created|e }}</div>
    <div class="post-content">{{ post.content|e }}</div>
	
{% endblock %}